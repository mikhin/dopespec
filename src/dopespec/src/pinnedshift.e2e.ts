import { test } from 'vitest';

test('PinnedShift: editAllFuture flow (active → active)', async () => {
  // TODO: setup — create PinnedShift in 'active' state
  // TODO: act — trigger editAllFuture
  // TODO: assert — verify state is 'active'
});

test('PinnedShift: editThisOnly flow (active → active)', async () => {
  // TODO: setup — create PinnedShift in 'active' state
  // TODO: act — trigger editThisOnly
  // TODO: assert — verify state is 'active'
});

test('PinnedShift: deleteOccurrence flow (active → active)', async () => {
  // TODO: setup — create PinnedShift in 'active' state
  // TODO: act — trigger deleteOccurrence
  // TODO: assert — verify state is 'active'
});

test('PinnedShift: unpin flow (active → deleted)', async () => {
  // TODO: setup — create PinnedShift in 'active' state
  // TODO: act — trigger unpin
  // TODO: assert — verify state is 'deleted'
});

test('PinnedShift: delete flow (active → deleted)', async () => {
  // TODO: setup — create PinnedShift in 'active' state
  // TODO: act — trigger delete
  // TODO: assert — verify state is 'deleted'
});
