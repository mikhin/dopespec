import { getActions, getTransitions } from "./utils.js";
/**
 * Aggregate Mermaid feature map across every construct, grouped by area.
 * Each construct is a node; its actions / transitions / rules are leaves.
 * Complements the per-construct diagrams produced by `generate`.
 */
export const generateFeatureMap = (entries, options = {}) => {
    const title = options.title ?? "Feature map";
    const order = [];
    const byArea = new Map();
    for (const { area, def } of entries) {
        let bucket = byArea.get(area);
        if (!bucket) {
            bucket = [];
            byArea.set(area, bucket);
            order.push(area);
        }
        bucket.push(def);
    }
    const lines = ["graph LR", `  ROOT(("${escape(title)}"))`, ""];
    const areaIds = [];
    const kindIds = {
        decision: [],
        model: [],
        policy: [],
    };
    const leafIds = [];
    let counter = 0;
    const nextId = () => `n${String(counter++)}`;
    for (const area of order) {
        const areaId = nextId();
        areaIds.push(areaId);
        lines.push(`  ROOT --> ${areaId}["${escape(area)}"]`);
        for (const def of byArea.get(area) ?? []) {
            const nodeId = nextId();
            kindIds[def.kind]?.push(nodeId);
            lines.push(`  ${areaId} --> ${nodeId}["${escape(def.name)} · ${kindWord(def.kind)}"]`);
            for (const leaf of leavesOf(def)) {
                const leafId = nextId();
                leafIds.push(leafId);
                lines.push(`  ${nodeId} --> ${leafId}["${escape(leaf)}"]`);
            }
        }
        lines.push("");
    }
    lines.push("  classDef root fill:#1a0dab,stroke:#0b0a6b,color:#ffffff,font-weight:bold;", "  classDef area fill:#e8f0fe,stroke:#4285f4,stroke-width:2px,color:#1a2024,font-weight:bold;", "  classDef model fill:#fef7e0,stroke:#f9ab00,stroke-width:2px,color:#1a2024;", "  classDef decision fill:#e6f4ea,stroke:#34a853,stroke-width:2px,color:#1a2024;", "  classDef policy fill:#fce8e6,stroke:#ea4335,stroke-width:2px,color:#1a2024;", "  classDef leaf fill:#f5f5f5,stroke:#9aa0a6,color:#1a2024;", "  class ROOT root;");
    assignClass(lines, areaIds, "area");
    assignClass(lines, kindIds["model"] ?? [], "model");
    assignClass(lines, kindIds["decision"] ?? [], "decision");
    assignClass(lines, kindIds["policy"] ?? [], "policy");
    assignClass(lines, leafIds, "leaf");
    const diagram = lines.join("\n");
    return `# ${title}

<!-- Generated by \`dopespec map\` — grouped by area, one node per construct. -->

\`\`\`mermaid
${diagram}
\`\`\`
`;
};
function assignClass(lines, ids, cls) {
    if (ids.length)
        lines.push(`  class ${ids.join(",")} ${cls};`);
}
const escape = (s) => s.replace(/"/g, "'");
function kindWord(kind) {
    return kind === "decision" ? "decisions" : kind;
}
function leavesOf(def) {
    if (def.kind === "model") {
        const actions = getActions(def).map(([key]) => key);
        if (actions.length)
            return actions;
        return getTransitions(def).map(([key]) => key);
    }
    if (def.kind === "decision") {
        return def.rules.map((rule) => {
            const r = rule;
            return `${pairs(r.when) || "*"} → ${pairs(r.then)}`;
        });
    }
    const effects = [...new Set(def.rules.map((rule) => rule.effect))].join("/");
    return [`${effects}: ${def.on.model.name}.${def.on.action}`];
}
function pairs(obj) {
    return Object.entries(obj)
        .map(([key, value]) => `${key}=${String(value)}`)
        .join(", ");
}
//# sourceMappingURL=feature-map.js.map